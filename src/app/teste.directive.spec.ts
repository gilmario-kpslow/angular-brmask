import { TesteDirective } from './teste.directive';

describe('TesteDirective', () => {
  it('should create an instance', () => {
    const directive = new TesteDirective();
    expect(directive).toBeTruthy();
  });
});
